# Lista de Tareas: Alquileres y Ventas (MiAlqui)

- [x] Agregar el selector de tipo `type-switcher` en la cabecera de `index.html`
- [x] Agregar el campo "Tipo de Publicación" (Alquiler / Venta) en el formulario de administrador de `index.html`
- [x] Definir los estilos CSS del selector `type-switcher` y ajustar responsividad en `style.css`
- [x] Actualizar los datos iniciales de `mockData.js` con el campo `type` y ajustar precios para ventas
- [x] Inicializar la variable de estado `currentType` y configurar la actualización dinámica del buscador de precios en `app.js`
- [x] Actualizar el filtrado de propiedades en `renderProperties()` y `updateMapMarkers()` según `currentType` en `app.js`
- [x] Adaptar dinámicamente los sufijos de precio (`/ mes` en alquileres, sin sufijo en ventas) en el catálogo, popups y modal en `app.js`
- [x] Adaptar el mensaje del enlace de WhatsApp para que diga "comprar" o "alquilar" según la propiedad en `app.js`
- [x] Actualizar el panel de administrador (formulario CRUD y tabla) para soportar el campo `type` en `app.js`
- [x] Realizar pruebas de verificación final (filtrado, cambio dinámico del buscador, guardado y enlaces de WhatsApp)
